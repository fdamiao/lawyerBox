//$(function(){
//	var placeholderelemente=$('#pacehoderelemente');
//	$('button[data-toggle="ajax-modal"]').click(function(Event)
//	{
//		var url=$(this).data('url');
//		$.get(url).done(function(data){
//			placeholderelemente.html(data);
//			placeholderelemente.find('.modal').modal('show');
//		})
//	})
		
	//$(function () {
	//	$('button[data-toggle="ajax-modal"]').click(function (event) {
	//		alert('button clicked');
	//	});
	//});

	//$(function () {
	//	$('button[data-toggle="ajax-modal"]').click(function (event) {
	//		// url to Razor Pages handler which returns modal HTML
	//		var url = '/Index?handler=criarPartial';
	//		$.get(url).done(function (data) {
	//			// append HTML to document, find modal and show it
	//			$(document.body).append(data).find('.modal').modal('show');
	//		});
	//	});
	//});
